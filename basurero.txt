
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass


// Retrive the data of the session.JSON file and append the new user
fs.readFile('source/session.json', (err, data) => {
    if (err) throw err;
    // Getting the .JSON module/s of session.JSON and converting it/them into an array to append the current user information
    let json = JSON.parse(data);                    
    json.push(session);
    
    // After appending the user to the array turn it back again to a .JSON module
    fs.writeFile("source/session.json", JSON.stringify(json, undefined, 2), err => {
        if (err) throw err;
    });
});


if (role == "" || role == null) {
            res.sendStatus(404);
            res.json({
                message: 
            })
        }


        ////

User.updateOne({ name: name }, {$set: {classes: }}) // finnish...